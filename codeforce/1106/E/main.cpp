#include <iostream>

struct Envelop { int s, t, d, w; };

int N, M, K;

int main() {
	scanf("%d %d %d", &N, &M, &K);
	for (int k = 0; k < K; k++) {
		int s, t, d, w;
		scanf("%d %d %d %d", &s, &t, &d, &w);

		
	}

	printf("%d\n", 0);

	return 0;
}
