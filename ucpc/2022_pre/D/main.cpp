#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

    int Q;
    cin >> Q;

    for (int q = 0; q < Q; q++) {
        int cmd;
        cin >> cmd;

        if (cmd == 1) {
            cin >> a >> b;
        } else {

        }
    }

    cout << '\n';

    return 0;
}
