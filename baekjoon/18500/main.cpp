#include <algorithm>
#include <cstdio>

using namespace std;

char grid[102][102];

int main() {
    int R, C;
    scanf("%d %d", &R, &C);

    for (int r = 0; r < R; r++) {
        scanf("%s", grid[r]);
    }

    int N, H;
    scanf("%d %d", &N, &H);

    return 0;
}
