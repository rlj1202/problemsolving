#include <iostream>

int N;

int main() {
	// read input
	scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		int x, y;
		scanf("%d %d", &x, &y);
	}

	// print result
	int result = 0;
	printf("%d\n", result);

	return 0;
}
