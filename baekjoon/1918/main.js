const input = require('fs').readFileSync(0).toString().trim();

const stack = [];

for (let i = 0; i < input.length; i++) {
    const char = input[i];

    if (char) {
        Buffer.from().equals()
    }
}
