#include <iostream>

int N, M;

int max_tasks;

int main() {
	scanf("%d %d", &N, &M);
	for (int n = 1; n <= N; n++) {
		int tasks;
		scanf("%d", &tasks);

		while (tasks--) {
			int task;
			scanf("%d", &task);


		}
	}

	printf("%d\n", max_tasks);

	return 0;
}
