#include <cstdio>

int main() {
	int T;
	scanf("%d", &T);

	while (T--) {
		int N, K, I;
		scanf("%d %d %d", &N, &K, &I);

		for (int k = 0; k < K; k++) {
			int x1, y1, x2, y2, weight;
			scanf("%d %d %d %d %d", &x1, &y1, &x2, &y2, &weight);
		}
	}

	return 0;
}
