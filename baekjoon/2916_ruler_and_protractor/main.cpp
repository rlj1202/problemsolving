#include <cstdio>

int N, K;


int main() {
	scanf("%d %d", &N, &K);
	for (int n = 0; n < N; n++) {
		int angle;
		scanf("%d", &angle);
	} 

	for (int k = 0; k < K; k++) {
		int angle;
		scanf("%d", &angle);

		printf("%s\n", "");
	}

	return 0;
}
