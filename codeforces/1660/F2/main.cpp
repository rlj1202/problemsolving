#include <cstdio>
#include <cstring>

using namespace std;

char str[200005];

int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        int N;
        scanf("%d", &N);
        scanf("%s", str);

        printf("%lld\n", 0);
    }

    return 0;
}
